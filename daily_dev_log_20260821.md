# Daily Development Report — 21 สิงหาคม 2026

## เรื่องที่ 1: แก้หน้าภาพรวมทีมล้มทั้งหน้าเมื่อ refresh (Viewer not found)

- ประเภท: Bug Fix
- สาเหตุ: ตอน refresh หน้าตรง ๆ effect ของหน้า MyTeam ยิง `get_briefing_point_ledger` ก่อนที่ AuthContext จะบันทึก userId ลง apiService ทำให้ viewer เป็นค่าว่าง RPC ตอบ 400 "Viewer not found" และเพราะอยู่ใน Promise.all จึงพาข้อมูลทีมทั้งหน้าล้มไปด้วย
- สิ่งที่ทำ:
  - หน้า MyTeam และ Dashboard ส่ง viewerId ของตัวเองจาก useAuth ตรง ๆ ไม่พึ่งลำดับ effect
  - `getBriefingPointLedger` ไม่ยิง RPC เมื่อไม่มี viewer (คืนลิสต์ว่าง) และฝั่ง MyTeam ครอบ catch แยก — สมุดคะแนนล้มก็แสดงข้อมูลทีมส่วนที่เหลือได้
- ผลลัพธ์: refresh หน้าภาพรวมทีมแล้วข้อมูลขึ้นครบทุกครั้ง Frontend test ผ่าน 32/32, coverage 100%, build ผ่าน

## เรื่องที่ 2: ช่องขยายเวลาเติมจำนวนวันที่เลยกำหนดให้อัตโนมัติ

- ประเภท: UX / พัฒนาฟีเจอร์
- สิ่งที่ทำ: เพิ่ม `getOverdueDays()` คำนวณจำนวนวันที่งานเลยกำหนดส่ง ณ วันนี้ตามเวลาไทย แล้วเติมลงช่อง "ขยายเวลาทั้งงาน (วัน)" ในหน้าตรวจงานให้อัตโนมัติ พร้อมป้ายแดง "เลยกำหนด X วัน" ข้างช่อง — หัวหน้าแผนกหรือ Admin แค่กดปุ่มขยายยืนยัน (แก้ตัวเลขเองได้ตามเดิม งานที่ยังไม่เลยกำหนดขึ้นค่าเริ่มต้น 1 วัน)
- ผลลัพธ์: ขยายเวลาให้งานที่ช้าได้พอดีจำนวนวันโดยไม่ต้องนับเอง Frontend test ผ่าน 33/33, function coverage 100%, build ผ่าน

## เรื่องที่ 3: หน้าตรวจงานแสดงลิงก์ผลงานและหมายเหตุของผู้ส่งงาน

- ประเภท: Bug Fix (สำคัญ)
- สาเหตุ: ส่วน "ผู้รับงานและหลักฐาน" ในหน้าตรวจงานแสดงเฉพาะหมายเหตุและรูปมาตั้งแต่แรก ไม่เคย render ลิงก์ผลงาน (URL1/URL2) — ตรวจข้อมูลจริงพบ 576 จาก 635 งานที่ส่ง ส่งเป็นลิงก์ล้วนไม่มีรูป หัวหน้าจึงเห็นเหมือนไม่มีหลักฐานเกือบทุกงาน (ไม่มีข้อมูลหาย ไม่มีใครลบ — โค้ดส่วนรูปเหมือนเดิมทุก commit)
- สิ่งที่ทำ:
  - แสดงลิงก์ผลงานทั้งสองช่องเป็นปุ่มกดเปิดแท็บใหม่ ผ่าน `normalizeExternalLink` เปิดเฉพาะ http/https (ลิงก์ไม่ปลอดภัยแสดงเป็นข้อความเฉย ๆ)
  - หมายเหตุการส่งงานแสดงตามเดิม และเพิ่มข้อความ "ส่งงานแล้วแต่ยังไม่มีหลักฐานแนบ" เมื่อ response ไม่มีทั้งรูป ลิงก์ และหมายเหตุ
- ผลลัพธ์: หัวหน้าเห็นหลักฐานครบทุกรูปแบบที่ผู้รับงานส่ง Frontend test ผ่าน 33/33, coverage 100%, build ผ่าน

## เรื่องที่ 4: จัดระเบียบรายงานประจำวันของ 20 สิงหาคม

- ประเภท: เอกสาร
- สิ่งที่ทำ: จัดส่วนที่บันทึกระหว่างวันให้เป็นหัวข้อเลขต่อเนื่อง (เรื่องที่ 10–20) ในรูปแบบเดียวกับรายงานวันอื่น เพิ่มผลตรวจคะแนนเดือนกรกฎาคมและสรุปการทดสอบ/deploy และหมายเหตุว่า migration ทุกไฟล์รันบน production ครบแล้ว
- ผลลัพธ์: รายงานวันที่ 20 สิงหาคม ใช้ส่งต่อได้ครบถ้วนในไฟล์เดียว

## เรื่องที่ 5: การทดสอบและ Deploy

- ประเภท: Quality Assurance
- ผลลัพธ์:
  - Frontend test ผ่าน 33/33 รายการ function coverage 100% (เกณฑ์ 90) | Backend test ผ่าน 34/34 รายการ coverage 94.64%
  - ESLint ผ่านทุกไฟล์ใน Git และ production build ผ่านทุกรอบ
  - ตรวจข้อมูลจริงบน production ก่อนแก้: การส่งงาน 635 รายการ เป็นลิงก์ล้วน 576 รายการ มีรูป 3 รายการ มีหมายเหตุ 13 รายการ ไม่มีแถวซ้ำ
  - Push ขึ้น `main` และ `dev` (commit เดียวกัน) รวม 5 commits: 67e58bc → 64285b6
