# Daily Development Report — 22 สิงหาคม 2026

## เรื่องที่ 1: ปุ่ม “เริ่มงาน” ฝั่งผู้รับงาน

- ประเภท: พัฒนาฟีเจอร์ใหม่ / Workflow + Database Migration
- สิ่งที่ทำ:
  - เพิ่ม RPC `start_briefing_work` (`20260820_briefing_start_work.sql` รันบน production แล้ว): ตรวจว่าเป็นผู้รับงานจริง ปฏิเสธงานที่ส่งตรวจ/เสร็จสิ้น/ยกเลิกแล้ว บันทึกสถานะของตนเองเป็น “กำลังทำ” (คงหลักฐานที่เคยบันทึกไว้) แล้วเปลี่ยนสถานะภาพรวมของบรีฟเป็น “กำลังทำ” พร้อม `LastUpdatedBy` ให้ Realtime แจ้งผู้มอบหมาย
  - หน้าบรีฟฝั่งผู้รับงาน: ปุ่ม “เริ่มงาน — แจ้งผู้มอบหมายว่ากำลังทำอยู่” แสดงเมื่อยังไม่ได้เริ่มและงานไม่ได้อยู่ในสถานะล็อก (`canStartBriefingWork`) กดแล้วรีเฟรชสถานะทันที
  - แปลข้อความปฏิเสธจากฟังก์ชันเป็นไทย และเพิ่มเทสต์ทั้งฝั่ง permission และไฟล์ migration
- ผลลัพธ์: ผู้มอบหมายเห็นทันทีว่างานถูกเริ่มแล้ว (สถานะกำลังทำ + toast realtime) โดยผู้รับงานยังแก้บรีฟไม่ได้เหมือนเดิม Frontend test 34/34 coverage 100%, Backend 35/35 coverage 94.64%, build ผ่าน

## เรื่องที่ 2: ตั้งค่าการเรียงลำดับหน้าบรีฟ (ใกล้กำหนดส่งก่อน / กำหนดส่งล่าสุดก่อน)

- ประเภท: UX / การตั้งค่าที่จดจำต่ออุปกรณ์
- สิ่งที่ทำ:
  - หน้าตั้งค่าการแสดงผลของหน้าบรีฟเพิ่มส่วน "การเรียงลำดับงาน (ทุกสถานะ)" 2 ตัวเลือก: กำหนดส่งใกล้สุดก่อน (ค่าเริ่มต้น) หรือ กำหนดส่งล่าสุดก่อน จำค่าใน `briefing_ui_settings` ร่วมกับมุมมองและจำนวนรายการ (อุปกรณ์เดิมได้ค่าเริ่มต้นอัตโนมัติ)
  - เพิ่ม `compareBriefingsByDueDateLatest` / `getBriefingComparator` ใน `briefingOrder.js` — ทิศทางที่เลือกมีผลกับรายการทุกสถานะ โดยยังคงกติกาเดิม: งานที่ยังไม่ปิดขึ้นก่อนงานที่ปิดแล้ว และงานไม่มีกำหนดส่งอยู่ท้ายสุด
- ผลลัพธ์: Frontend test ผ่าน 35/35, function coverage 100%, ESLint + build ผ่าน

## เรื่องที่ 3: ตอบคำถามการทำงานของระบบ (ไม่มีการแก้โค้ด)

- ประเภท: Q&A / ยืนยันพฤติกรรมระบบ
- สิ่งที่ตรวจและตอบ:
  - การ์ด “คะแนนสุทธิของฉัน (เดือนนี้)” บนแดชบอร์ดอิงเดือนปัจจุบันตามเวลาไทยแบบตายตัว (1 ถึงสิ้นเดือน) ทั้งฝั่งคะแนนที่ได้และฝั่งหัก ไม่ตามตัวกรองปี/วันที่ของแดชบอร์ดและไม่ใช่ยอดรวมทั้งหมด ใช้กติกาเดียวกับหน้าภาพรวมทีม
  - ตำแหน่งปุ่ม “เริ่มงาน”: หน้ารายละเอียดบรีฟ → ส่วนการส่งมอบงาน → เหนือปุ่มบันทึกส่งงาน แสดงเฉพาะผู้รับงานที่เลือกแท็บชื่อตัวเอง และงานยังไม่อยู่ในสถานะ กำลังทำ/ส่งตรวจ/เสร็จสิ้น/ยกเลิกงาน

## เรื่องที่ 4: การทดสอบและ Deploy

- ประเภท: Quality Assurance
- ผลลัพธ์:
  - Frontend test ผ่าน 35/35 รายการ function coverage 100% (เกณฑ์ 90) | Backend test ผ่าน 35/35 รายการ coverage 94.64%
  - ESLint ผ่านทุกไฟล์ใน Git และ production build ผ่านทุกรอบ
  - Migration `20260820_briefing_start_work.sql` รันบน production แล้วและตรวจว่า function ถูกสร้างครบ
  - Push ขึ้น `main` และ `dev` (commit เดียวกัน) รวม 2 commits: 2dbe0f8 → 711de54 — Vercel deploy จาก 711de54
