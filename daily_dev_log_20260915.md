# Daily Development Report — 15 กันยายน 2026

## เรื่องที่ 1: คนที่บรีฟงานให้ตัวเองแก้วันที่บรีฟไม่ได้

- ประเภท: แก้บั๊ก / สิทธิ์การแก้ไข
- ปัญหาที่พบ:
  - ผู้บรีฟที่ใส่ชื่อตัวเองเป็นผู้รับงานด้วย (เช่น BR-2609-809, BR-2609-810) เปิดบรีฟแล้วช่องวันสิ้นสุด ชื่องาน รายละเอียด คะแนน และปุ่ม “บันทึกข้อมูลงาน” ถูกปิดหมด งานจึงค้างป้าย “ล่าช้า (LATE)”
  - สาเหตุ: `canEditBriefingContent()` เช็ค `isRecipientOnly()` ก่อนดูว่าเป็นผู้สร้าง ระบบจึงล็อกผู้สร้างเหมือนผู้รับงานทั่วไป กฎเดียวกันนี้เคยแก้ให้ปุ่มสถานะและตัวกันใน `api.js` แล้ว (ac9e5db, 2cf583a) แต่ส่วนเนื้อหาบรีฟยังไม่ได้แก้ และมีเทสยืนยันพฤติกรรมที่ผิดอยู่
- สิ่งที่ทำ:
  - `briefingPermissions.js`: ผู้สร้างแก้เนื้อหาบรีฟของตัวเองได้เสมอ ส่วนผู้รับงานคนอื่น รวมแอดมินและหัวหน้าแผนก ยังแก้ไม่ได้
  - `BriefingModal.jsx`: ปุ่ม “บันทึกรายละเอียดการส่งงาน” ผูกกับการเปิดช่องส่งงานของตัวเอง แทน `!canEditBrief` เพื่อไม่ให้ปุ่มหายเมื่อคนเดียวเป็นทั้งผู้บรีฟและผู้รับ
  - แก้เทสที่ยืนยันพฤติกรรมผิด และเพิ่มเคสว่าผู้ช่วยในบรีฟเดียวกันยังถูกล็อก
- ผลลัพธ์: ผู้บรีฟตัวเองแก้วันสิ้นสุดได้ ป้าย LATE หายเมื่อเลื่อนวันส่ง Frontend test 60/60, Backend 63/63, build ผ่าน — commit 52ff65f

## เรื่องที่ 2: คนที่บรีฟงานให้ตัวเองลบบรีฟตัวเองไม่ได้

- ประเภท: แก้บั๊ก / สิทธิ์การลบ
- ปัญหาที่พบ: ปุ่มลบในมุมมองตารางและการ์ดเช็ค `!isRecipientOnly()` เหมือนเรื่องที่ 1 และเขียนเงื่อนไขซ้ำไว้ 2 ที่ ส่วน `deleteBriefing` ใน `api.js` ยอมให้ผู้สร้างลบได้อยู่แล้ว ปัญหาจึงอยู่ที่หน้าจอไม่แสดงปุ่มเท่านั้น
- สิ่งที่ทำ:
  - เพิ่ม `canDeleteBriefingRecord()` เป็นกฎการลบที่เดียว ใช้หลักเดียวกับการแก้เนื้อหาบรีฟ: ผู้สร้าง หรือแอดมิน/หัวหน้าแผนกที่ไม่ได้รับงานนั้น
  - มุมมองตารางและการ์ดใน `Briefing.jsx` เรียกกฎเดียวกัน
  - เพิ่มเทส: ผู้บรีฟตัวเองลบได้, ผู้ช่วยในบรีฟเดียวกันลบไม่ได้แม้เป็นแอดมิน, แอดมิน/หัวหน้าที่ไม่ได้รับงานยังลบได้
- ผลลัพธ์: ผู้บรีฟตัวเองเห็นและใช้ปุ่มลบบรีฟของตัวเองได้ — commit 0a34451

## เรื่องที่ 3: ประเด็นที่ยังเปิดอยู่ (ไม่ได้แก้ในรอบนี้)

- เลี่ยงโทษส่งช้า: เมื่อผู้บรีฟตัวเองเลื่อนวันส่งได้เอง ก็เลื่อนหนีโทษส่งช้าได้ (โทษคิดจาก `DueDate` ตอนตรวจงาน) ทางเลือกคือห้ามเลื่อนหลังเลยกำหนด หรือบังคับให้เลื่อนผ่านปุ่มขยายเวลาในหน้าตรวจงานที่มีบันทึกประวัติ — ผู้ใช้ยังไม่ให้แก้
- เลขบรีฟซ้ำ (`BR-2609-810` มี 2 ใบ) จากตัวสร้างเลขแบบ `count(*) + 1` — ยังไม่ได้แก้

## เรื่องที่ 4: การทดสอบและ Deploy

- ประเภท: Quality Assurance
- ผลลัพธ์:
  - Frontend test ผ่าน 61/61 รายการ line coverage 98.47%, function coverage 100% (เกณฑ์ 90) — `briefingPermissions.js` 96.10%
  - Backend test ผ่าน 63/63 รายการ coverage 91.13%
  - Production build ผ่านทุกรอบ
  - Push ขึ้น `main` และ `dev` (commit เดียวกัน) รวม 2 commits: 9aa9bf6 → 52ff65f → 0a34451
